<section class="hero">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        投稿編集
      </h1>
    </div>
  </div>
</section>

<% if @post.errors.any? %>
  <div class="notification">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited this object from being saved: not successfully</h2>
    <ul>
      <% @post.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-6">
        <%= form_for @post do |f| %>
          <div class="field">
            <%= f.label :title, "曲名", class: "label" %>
            <%= f.text_field :title, class: "input" %>
          </div>

          <div class="field">
            <%= f.label :lyrics, "歌詞", class: "label" %>
            <%= f.text_area :lyrics, class: "textarea" %>
          </div>

          <div class="field">
            <%= f.label :artist, "歌手名", class: "label" %>
            <%= f.text_field :artist, class: "input" %>
          </div>

          <%= f.submit '更新', class: "button is-light" %>
        <% end %>
      </div>
    </div>
  </div>
</section>
